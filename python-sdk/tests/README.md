## Overview

Put here unit tests and integration tests for Python SDK.
This package is particularly a good fit for end to end
integration tests that call Python SDK -> Indexify Server ->
Executor -> Function Executor. Start Indexify Server and an
Executor before running these tests.